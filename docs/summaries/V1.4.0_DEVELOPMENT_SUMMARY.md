# DeepFileX v1.4.0 개발 요약

**개발 기간**: 2026-02-08
**릴리즈 날짜**: 2026-02-08
**개발자**: QuantumLayer
**주요 목표**: GitHub Pages 기반 Adsterra 광고 시스템 구현

---

## 📋 목차

1. [개발 배경](#개발-배경)
2. [주요 문제 및 해결 과정](#주요-문제-및-해결-과정)
3. [기술적 세부사항](#기술적-세부사항)
4. [파일 변경 사항](#파일-변경-사항)
5. [배포 프로세스](#배포-프로세스)
6. [테스트 결과](#테스트-결과)

---

## 개발 배경

### 기존 문제점 (v1.3.0)
1. **Coupang Partners 배너**: 플레이스홀더 이미지만 표시, 실제 광고 이미지 없음
2. **Rotating Image Banner**: 그라데이션 플레이스홀더만 표시
3. **SmartLinks**: 텍스트 배너만 표시, 이미지 없음

### 사용자 요구사항
- ✅ 실제 광고 이미지 표시
- ✅ 광고 자동 회전
- ✅ 광고 클릭 시 구매/광고 페이지로 이동
- ✅ 프로그램 내부에서 직접 표시 (외부 브라우저 창 X)

---

## 주요 문제 및 해결 과정

### 문제 1: QWebEngineView DLL 로드 실패

**증상**:
```
ImportError: DLL load failed while importing QtWebEngineWidgets:
지정된 모듈을 찾을 수 없습니다.
```

**원인**:
- PyQt6 6.9.1과 PyQt6-WebEngine 6.10.0 버전 불일치
- Qt6 바이너리 간 호환성 문제

**해결 과정**:
1. **시도 1**: PyQt6-WebEngine 다운그레이드 (6.10.0 → 6.7.0)
   - 결과: 실패, 여전히 DLL 오류

2. **시도 2**: Visual C++ Redistributable 확인
   - 결과: 이미 설치되어 있음 (2022 버전)

3. **최종 해결**: PyQt6 전체 업그레이드
   ```bash
   pip install --upgrade PyQt6
   # 6.9.1 → 6.10.2
   ```
   - 결과: ✅ 성공! DLL 로드 정상화

**교훈**:
- PyQt6와 PyQt6-WebEngine은 메이저.마이너 버전이 일치해야 함
- 6.10.x끼리만 호환됨

---

### 문제 2: 광고 클릭 시 브라우저가 열리지 않음

**증상**:
- 광고 클릭 시 아무 반응 없음
- 네비게이션 요청이 인터셉트되지 않음

**원인**:
- 기본 QWebEnginePage는 외부 링크를 처리하지 않음
- JavaScript `window.open()` 호출이 무시됨

**해결 과정**:

1. **커스텀 페이지 클래스 생성**:
   ```python
   class AdWebEnginePage(QWebEnginePage):
       def acceptNavigationRequest(self, url, nav_type, is_main_frame):
           # GitHub Pages 광고 페이지는 허용
           if "noblejim.github.io/DeepFileX/ads" in url_str:
               return True

           # 모든 외부 링크는 브라우저로 열기
           QDesktopServices.openUrl(url)
           return False
   ```

2. **JavaScript 팝업 처리**:
   ```python
   def createWindow(self, window_type):
       # 새 창 대신 현재 페이지에서 URL을 acceptNavigationRequest로 전달
       return self
   ```

3. **추가 설정**:
   ```python
   settings.setAttribute(
       QWebEngineSettings.WebAttribute.JavascriptCanOpenWindows,
       True
   )
   ```

**결과**: ✅ 광고 클릭 시 브라우저 정상 오픈

---

### 문제 3: 광고 클릭 후 배너가 사라짐

**증상**:
- 광고 클릭 후 프로그램 내 배너 영역이 비어버림
- QWebEngineView가 빈 페이지 상태로 남음

**원인**:
- `acceptNavigationRequest()`에서 `return False` 후 페이지가 비어버림
- 원래 광고 페이지로 돌아가는 로직 없음

**해결**:
```python
def acceptNavigationRequest(self, url, nav_type, is_main_frame):
    # 외부 링크 브라우저로 열기
    QDesktopServices.openUrl(url)

    # 100ms 후 원래 광고 페이지로 리로드
    from PyQt6.QtCore import QTimer
    QTimer.singleShot(100, lambda: self.setUrl(QUrl(self.ad_page_url)))

    return False
```

**결과**: ✅ 광고 클릭 후에도 배너 계속 표시

---

### 문제 4: 광고 이미지가 잘림

**증상**:
- 광고 이미지의 아래 부분이 잘려서 보임
- 전체 광고 내용을 볼 수 없음

**해결 과정**:

**1차 시도**: 90px → 140px (+50px)
- 결과: 여전히 잘림

**2차 시도**: 140px → 190px (+50px)
- 결과: 여전히 잘림

**3차 시도**: 190px → 240px (+50px)
- 결과: ✅ 성공! 전체 이미지 표시

**최종 크기**:
```python
# HTML (docs/ads/index.html)
.ad-container { height: 250px; }
.ad-wrapper { height: 240px; }

# Python (github_pages_ad_widget.py)
self.setFixedHeight(260)
self.web_view.setFixedSize(970, 240)
```

---

## 기술적 세부사항

### 아키텍처

```
DeepFileX (filemri.py)
    ↓
GitHubPagesAdWidget
    ↓
QWebEngineView + AdWebEnginePage
    ↓
GitHub Pages (noblejim.github.io/DeepFileX/ads/)
    ↓
Adsterra Banner Ad JavaScript
    ↓
실제 광고 이미지 표시
```

### 데이터 흐름

1. **프로그램 시작**:
   - `filemri.py` → `GitHubPagesAdWidget` 초기화
   - QWebEngineView 생성 및 설정

2. **광고 로드**:
   - `https://noblejim.github.io/DeepFileX/ads/` 로드
   - Adsterra JavaScript 실행
   - 광고 이미지 동적 삽입

3. **광고 클릭**:
   - 클릭 이벤트 발생
   - `AdWebEnginePage.acceptNavigationRequest()` 호출
   - 외부 URL을 `QDesktopServices.openUrl()`로 브라우저에서 열기
   - 100ms 후 `QTimer`로 광고 페이지 리로드

4. **네비게이션 타입**:
   - `NavigationTypeTyped`: 최초 페이지 로드 → 허용
   - `NavigationTypeLinkClicked`: 광고 클릭 → 브라우저로 열기
   - `WebBrowserTab`: JavaScript window.open() → createWindow() 처리

### 주요 클래스 및 메서드

#### `AdWebEnginePage`
```python
class AdWebEnginePage(QWebEnginePage):
    def __init__(self, parent=None):
        super().__init__(parent)
        self.ad_page_url = "https://noblejim.github.io/DeepFileX/ads/"

    def acceptNavigationRequest(self, url, nav_type, is_main_frame):
        """네비게이션 요청 인터셉터"""

    def createWindow(self, window_type):
        """JavaScript 팝업 처리"""
```

#### `GitHubPagesAdWidget`
```python
class GitHubPagesAdWidget(QFrame):
    def __init__(self, parent=None, location="bottom_banner"):
        """광고 위젯 초기화"""

    def init_ui(self):
        """QWebEngineView 생성 및 설정"""

    def track_impression(self):
        """노출 추적"""
```

---

## 파일 변경 사항

### 신규 파일

1. **`src/github_pages_ad_widget.py`** (150줄)
   - GitHub Pages 광고 위젯
   - QWebEngineView 기반
   - 외부 링크 처리 로직

2. **`docs/ads/index.html`** (130줄)
   - Adsterra 배너 호스팅 페이지
   - 970×240 광고 컨테이너
   - JavaScript 추적 코드

3. **`docs/ads/README.md`** (설정 가이드)
   - GitHub Pages 활성화 방법
   - Adsterra 코드 삽입 방법
   - 문제 해결 가이드

4. **`DeepFileX_v1.4.0.spec`** (PyInstaller 설정)
   - 빌드 설정 파일
   - 의존성 정의
   - 데이터 파일 포함

5. **`build_v1.4.0.bat`** (빌드 스크립트)
   - 자동 빌드 워크플로우
   - 의존성 설치
   - 릴리즈 폴더 생성

6. **`RELEASE_NOTES_v1.4.0.md`** (릴리즈 노트)
   - 상세 변경사항
   - 설치 가이드
   - 시스템 요구사항

### 수정된 파일

1. **`src/version_config.py`**
   - VERSION: "1.3.0" → "1.4.0"
   - VERSION_CODE: 130 → 140
   - BUILD_DATE: "2026-02-08"
   - test_mode: True → False
   - GitHub URL: quantumlayer → noblejim
   - v1.4.0 변경사항 추가

2. **`src/filemri.py`**
   - 광고 시스템 import 우선순위 변경:
     ```python
     try:
         from github_pages_ad_widget import GitHubPagesAdWidget as AdBanner
     except:
         from rotating_image_banner import RotatingImageBanner as AdBanner
     except:
         from filemri_smartlinks import SmartLinksAdWidget as AdBanner
     ```

3. **`CHANGELOG.md`**
   - v1.4.0 섹션 추가
   - 상세 변경사항 기록

4. **`docs/ads/index.html`**
   - Adsterra 광고 코드 삽입
   - 컨테이너 높이 증가 (90px → 240px)

### 의존성 변경

**업그레이드**:
```
PyQt6: 6.9.1 → 6.10.2
PyQt6-Qt6: 6.9.1 → 6.10.2
PyQt6-WebEngine: 6.7.0 → 6.10.0
PyQt6-WebEngine-Qt6: 6.7.3 → 6.10.2
```

**새 요구사항**:
- Visual C++ Redistributable 2022 (이미 설치됨)
- 인터넷 연결 (GitHub Pages 및 광고 로드)

---

## 배포 프로세스

### 1. 버전 업데이트
```bash
# version_config.py 수정
VERSION = "1.4.0"
test_mode = False
GitHub URL 수정
```

### 2. 빌드
```bash
# 빌드 스크립트 실행
build_v1.4.0.bat

# 또는 수동:
python -m PyInstaller DeepFileX_v1.4.0.spec --clean --noconfirm
```

**빌드 시간**: 약 2분
**출력 크기**: 222 MB

### 3. 테스트
```bash
# 실행 파일 테스트
releases/v1.4.0/DeepFileX_v1.4.0.exe
```

**테스트 항목**:
- [x] 프로그램 시작
- [x] GitHub Pages 광고 로드
- [x] 광고 이미지 표시
- [x] 광고 클릭 → 브라우저 열림
- [x] 광고 클릭 후 배너 유지
- [x] 파일 검색 기능

### 4. Git 태그 및 푸시
```bash
git tag -a v1.4.0 -m "DeepFileX v1.4.0 - GitHub Pages Ad System"
git push origin v1.4.0
```

### 5. GitHub Release
1. https://github.com/noblejim/DeepFileX/releases/new?tag=v1.4.0
2. 릴리즈 정보 입력
3. `DeepFileX_v1.4.0.exe` 업로드
4. Publish release

---

## 테스트 결과

### 기능 테스트 (2026-02-09 00:08)

**실행 로그**:
```
00:08:25 - GitHub Pages ad banner system loaded (Adsterra hosted)
00:08:25 - 업데이트 시스템 로드 성공
00:08:27 - GitHub Pages ad widget loaded: https://noblejim.github.io/DeepFileX/ads/
00:08:27 - SmartLinks 위젯 UI 통합 완료
00:08:28 - DeepFileX started successfully!
```

**광고 클릭 테스트 (00:09:16)**:
```
[AD] Window creation request, type: WebBrowserTab
Navigation request: https://cdn.cloudvideosa.com/...
[AD CLICK] Opening in browser: https://cdn.cloudvideosa.com/...
Navigation request: https://noblejim.github.io/DeepFileX/ads/ (reload)
```

### 성능 테스트

| 항목 | 결과 |
|------|------|
| 시작 시간 | ~2.5초 |
| 광고 로드 시간 | ~2초 |
| 광고 클릭 반응 | 즉시 |
| 광고 리로드 | 100ms |
| 메모리 사용량 | ~150MB |

### 호환성 테스트

- ✅ Windows 11 (64-bit)
- ✅ Python 3.13.3
- ✅ PyQt6 6.10.2
- ✅ 인터넷 연결 필수

---

## 배포 후 체크리스트

- [x] GitHub Release 생성
- [x] 실행 파일 업로드
- [x] 릴리즈 노트 작성
- [x] CHANGELOG.md 업데이트
- [x] 문서 업데이트
- [x] 태그 푸시
- [x] 자동 업데이트 시스템 활성화

---

## 향후 개선 사항

### 단기 (v1.4.x)
- [ ] 광고 통계 대시보드
- [ ] 광고 클릭률(CTR) 추적
- [ ] 다양한 광고 크기 지원

### 중기 (v1.5.0)
- [ ] 여러 광고 네트워크 지원
- [ ] A/B 테스팅 시스템
- [ ] 광고 수익 보고서

### 장기 (v2.0.0)
- [ ] 네이티브 광고 통합
- [ ] 프리미엄 구독 모델
- [ ] 광고 없는 유료 버전

---

## 참고 자료

### 문서
- [PyQt6 Documentation](https://www.riverbankcomputing.com/static/Docs/PyQt6/)
- [QWebEngineView](https://doc.qt.io/qt-6/qwebengineview.html)
- [Adsterra Documentation](https://adsterra.com/ad-formats/)

### 관련 파일
- `RELEASE_NOTES_v1.4.0.md`
- `docs/ads/README.md`
- `.claude/ralph-loop.local.md`

### 저장소
- GitHub: https://github.com/noblejim/DeepFileX
- Releases: https://github.com/noblejim/DeepFileX/releases
- Issues: https://github.com/noblejim/DeepFileX/issues

---

**문서 작성**: 2026-02-09
**작성자**: QuantumLayer
**버전**: 1.0
