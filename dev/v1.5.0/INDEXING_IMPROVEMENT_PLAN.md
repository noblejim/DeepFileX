# 📋 인덱싱 시스템 개선 계획 (v1.5.0)

## 📊 현재 시스템 분석

### ✅ 현재 구현 (v1.4.1)
- **파일명**: `deepfilex_index_{folder_names}_{timestamp}.pkl`
  - 예: `deepfilex_index_Documents-Downloads_20260209_175923.pkl`
- **저장 위치**: `%AppData%\Roaming\DeepFileX\indexes\`
- **타임스탬프**: `YYYYMMDD_HHMMSS`
- **데이터 구조**:
  - `index_data`: 파일 메타데이터
  - `word_index`: 단어별 파일 인덱스
  - `filename_index`: 파일명 인덱스
  - `stats`: 스캔 통계
  - `version`: 인덱스 버전 (1.0, 1.1)
  - `created`: 생성 시간

### ❌ 현재 문제점
1. **파일명이 복잡하고 길음**
   - `deepfilex_index_Documents-Downloads_20260209_175923.pkl` (너무 김)
   - 로드맵 요구: `260209_1759_내문서.pkl` (간결함)


2. **기능 부재**
   - ❌ 인덱스 업데이트 (기존 수정 불가, 항상 새로 생성)
   - ❌ 인덱스 통합 (여러 개 → 하나)
   - ❌ 인덱스 비교 기능
   - ❌ 증분 업데이트
   - ❌ 인덱스 목록 관리 UI

3. **UI 문제**
   - 단순 파일 저장/로드 다이얼로그만 있음
   - 인덱스 목록을 볼 수 없음
   - 미리보기 없음
   - 태그/카테고리 없음

---

## 🎯 개선 목표 (v1.5.0)

### 1️⃣ 파일명 규칙 간소화
**현재**: `deepfilex_index_Documents-Downloads_20260209_175923.pkl`
**목표**: `260209_1759_내문서.pkl`

#### 새로운 규칙:
- 형식: `YYMMDD_HHMM_폴더명.pkl`
- 예시:
  - `260209_1759_내문서.pkl`
  - `260209_1802_Downloads.pkl`
  - `260210_0930_Projects.pkl`

#### 구현:
```python
def generate_index_filename(folder_path):
    timestamp = datetime.now().strftime('%y%m%d_%H%M')
    folder_name = os.path.basename(folder_path)
    # 한글 지원, 특수문자 제거
    safe_name = sanitize_folder_name(folder_name)
    return f"{timestamp}_{safe_name}.pkl"
```


### 2️⃣ 인덱스 업데이트 기능

**목적**: 기존 인덱스를 수정/업데이트 (새로 만들지 않음)

#### 기능:
- 폴더를 다시 스캔하여 기존 인덱스 업데이트
- 새 파일 추가
- 삭제된 파일 제거
- 수정된 파일 재인덱싱

#### 구현:
```python
def update_index(index_file, folder_paths):
    # 1. 기존 인덱스 로드
    old_index = load_index(index_file)
    
    # 2. 현재 파일 시스템 스캔
    current_files = scan_folders(folder_paths)
    
    # 3. 변경 사항 감지
    added = current_files - old_index.files
    removed = old_index.files - current_files
    modified = detect_modified_files(old_index, current_files)
    
    # 4. 인덱스 업데이트
    for file in added:
        add_to_index(file)
    for file in removed:
        remove_from_index(file)
    for file in modified:
        reindex_file(file)
    
    # 5. 저장 (동일한 파일명)
    save_index(index_file)
```


### 3️⃣ 인덱스 통합 기능

**목적**: 여러 인덱스 파일을 하나로 합치기

#### 사용 사례:
- `260209_1759_Documents.pkl` + `260209_1802_Downloads.pkl` 
  → `260209_1805_Documents+Downloads.pkl`

#### 구현:
```python
def merge_indexes(index_files, output_name=None):
    merged_data = {
        'index_data': {},
        'word_index': defaultdict(set),
        'filename_index': defaultdict(set),
        'stats': {'total_files': 0, 'total_size': 0}
    }
    
    for index_file in index_files:
        data = load_index(index_file)
        
        # 병합
        merged_data['index_data'].update(data['index_data'])
        
        for word, files in data['word_index'].items():
            merged_data['word_index'][word].update(files)
        
        for filename, paths in data['filename_index'].items():
            merged_data['filename_index'][filename].update(paths)
        
        # 통계 합산
        merged_data['stats']['total_files'] += data['stats']['total_files']
        merged_data['stats']['total_size'] += data['stats']['total_size']
    
    # 자동 파일명 생성
    if not output_name:
        folder_names = extract_folder_names(index_files)
        timestamp = datetime.now().strftime('%y%m%d_%H%M')
        output_name = f"{timestamp}_{'+'.join(folder_names)}.pkl"
    
    save_index(output_name, merged_data)
```


### 4️⃣ 인덱스 비교 기능

**목적**: 두 인덱스의 차이점 확인

#### 비교 항목:
- 추가된 파일
- 삭제된 파일
- 수정된 파일
- 통계 변화

#### 구현:
```python
def compare_indexes(index1_file, index2_file):
    idx1 = load_index(index1_file)
    idx2 = load_index(index2_file)
    
    files1 = set(idx1['index_data'].keys())
    files2 = set(idx2['index_data'].keys())
    
    comparison = {
        'added': files2 - files1,
        'removed': files1 - files2,
        'common': files1 & files2,
        'modified': []
    }
    
    # 수정 파일 감지 (크기, 수정일 비교)
    for file in comparison['common']:
        if idx1['index_data'][file] != idx2['index_data'][file]:
            comparison['modified'].append(file)
    
    return comparison
```

### 5️⃣ 인덱싱 UI 재설계

**목적**: 직관적인 인덱스 관리 인터페이스


#### 새로운 "인덱스 관리자" 창:

**레이아웃**:
```
┌─────────────────────────────────────────────────────────┐
│  📂 인덱스 관리자                           [새로고침] │
├─────────────────────────────────────────────────────────┤
│  인덱스 목록:                                           │
│  ┌───────────────────────────────────────────────────┐ │
│  │ ☑ 260209_1759_Documents.pkl      │ 1,234 files  │ │
│  │ ☐ 260209_1802_Downloads.pkl      │   567 files  │ │
│  │ ☐ 260208_1430_Projects.pkl       │   890 files  │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  미리보기:                                              │
│  ┌───────────────────────────────────────────────────┐ │
│  │ 파일명: 260209_1759_Documents.pkl                 │ │
│  │ 생성일: 2026-02-09 17:59:23                       │ │
│  │ 파일 수: 1,234개                                  │ │
│  │ 총 크기: 15.6 GB                                  │ │
│  │ 폴더: C:\Users\...\Documents                      │ │
│  │ 태그: [문서] [개인] [중요]                        │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  작업:                                                  │
│  [로드]  [업데이트]  [통합]  [비교]  [삭제]  [태그]   │
└─────────────────────────────────────────────────────────┘
```

#### 기능:
1. **인덱스 목록**
   - 모든 인덱스 파일 표시
   - 파일명, 생성일, 파일 수, 크기
   - 다중 선택 가능 (체크박스)

2. **미리보기**
   - 선택한 인덱스 상세 정보
   - 통계, 메타데이터
   - 태그 표시

3. **작업 버튼**
   - **로드**: 선택한 인덱스 로드
   - **업데이트**: 인덱스 갱신 (폴더 재스캔)
   - **통합**: 여러 인덱스 합치기
   - **비교**: 두 인덱스 차이 보기
   - **삭제**: 인덱스 삭제
   - **태그**: 태그 추가/수정


### 6️⃣ 태그/카테고리 시스템

**목적**: 인덱스를 분류하고 관리

#### 데이터 구조:
```python
index_metadata = {
    'filename': '260209_1759_Documents.pkl',
    'created': '2026-02-09T17:59:23',
    'folders': ['C:\\Users\\...\\Documents'],
    'tags': ['문서', '개인', '중요'],
    'category': '개인문서',
    'description': '개인 문서 폴더 인덱스',
    'stats': {...}
}
```

#### 태그 기능:
- 사용자 정의 태그 추가
- 미리 정의된 카테고리 (문서, 프로젝트, 미디어, 백업 등)
- 태그로 필터링
- 색상 코딩

---

## 🗓️ 구현 일정

### Week 1-2: 파일명 규칙 & 업데이트 기능
- [x] 현재 시스템 분석 완료
- [ ] 새 파일명 규칙 구현
  - `generate_index_filename()` 함수
  - 한글 지원 강화
  - 특수문자 처리
- [ ] 인덱스 업데이트 기능
  - `update_index()` 함수
  - 변경 감지 알고리즘
  - 증분 업데이트

### Week 3: 통합 & 비교 기능
- [ ] 인덱스 통합 기능
  - `merge_indexes()` 함수
  - 중복 제거 로직
  - 자동 파일명 생성
- [ ] 인덱스 비교 기능
  - `compare_indexes()` 함수
  - 차이점 시각화
  - 비교 리포트


### Week 4-5: UI 재설계
- [ ] "인덱스 관리자" 창 설계
  - PyQt6 UI 레이아웃
  - 인덱스 목록 테이블
  - 미리보기 패널
- [ ] 기능 통합
  - 로드/업데이트/통합/비교 버튼
  - 진행률 표시
  - 에러 처리

### Week 6: 태그 & 메타데이터
- [ ] 태그 시스템
  - 메타데이터 저장
  - 태그 편집 UI
  - 태그 필터링
- [ ] 카테고리 시스템
  - 미리 정의된 카테고리
  - 자동 분류 (폴더명 기반)
  - 색상 코딩

### Week 7: 테스트 & 최적화
- [ ] 단위 테스트
- [ ] 통합 테스트
- [ ] 성능 최적화
- [ ] 문서화

---

## 📝 기술 스택

### 백엔드
- Python 3.8+
- SQLite (인덱스 저장)
- Pickle (기존 호환성)
- JSON (메타데이터)

### 프론트엔드
- PyQt6 (UI)
- QTableWidget (인덱스 목록)
- QTextEdit (미리보기)
- QDialog (비교/통합 팝업)

---

## 🎯 성공 기준

### 필수 기능
- ✅ 파일명: `YYMMDD_HHMM_폴더명.pkl`
- ✅ 인덱스 업데이트 (기존 수정)
- ✅ 인덱스 통합 (여러 개 → 하나)
- ✅ 인덱스 비교
- ✅ 인덱스 관리자 UI

### 선택 기능
- ⭐ 태그 시스템
- ⭐ 카테고리 자동 분류
- ⭐ 증분 업데이트
- ⭐ 비교 리포트 내보내기

---

## 📚 참고 자료

### 현재 코드 위치
- `src/deepfilex.py:521-600` - TurboSearchEngine.save_index/load_index
- `src/deepfilex.py:1868-2000` - DeepFileXGUI.save_index/load_index
- `src/deepfilex.py:1904-1965` - 파일명 생성 로직

### 관련 이슈
- 파일명이 너무 길어서 읽기 어려움
- 인덱스 업데이트 불가능 (항상 새로 생성)
- 인덱스 관리 UI 부재

---

**작성일**: 2026-02-09
**작성자**: DeepFileX Dev Team
**상태**: 계획 단계
**다음 단계**: Week 1 개발 시작

